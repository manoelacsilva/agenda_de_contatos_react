class Contato {
  id: number
  name: string
  tell: string
  email: string

  constructor(id: number, name: string, tell: string, email: string) {
    this.id = id
    this.name = name
    this.tell = tell
    this.email = email
  }
}

export default Contato
